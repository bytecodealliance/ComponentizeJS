{
  "type": "module",
  "devDependencies": {
    "@bytecodealliance/componentize-js": "*"
  },
  "scripts": {
    "build": "node componentize.js && cargo build --release",
    "all": "npm run build"
  }
}
